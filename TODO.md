# TODO: Make Booking Table Rows Clickable

- [x] Modify the `<tr>` element for each booking row to add an `onclick` attribute that redirects to the booking show route
- [x] Add `style="cursor: pointer;"` to the `<tr>` for visual indication
- [x] Remove the "View" link from the Action column
- [x] Add `onclick="event.stopPropagation();"` to the checkbox in each row to prevent row click when checking/unchecking
- [x] Keep the "Edit" link in the Action column unchanged
